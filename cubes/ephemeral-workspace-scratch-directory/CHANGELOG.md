# Changelog

## v0.1.0

- native atomic unique workspace creation
- immutable workspace identity and bounded owner metadata
- EWC1 integrity-protected workspace records
- idempotent exact-owner cleanup
- symlink replacement and path-boundary protection
- optional TTL status without implicit deletion
- conservative explicit-token stale recovery
- deterministic clock, identity, and filesystem capability seams
- zero runtime third-party dependencies
