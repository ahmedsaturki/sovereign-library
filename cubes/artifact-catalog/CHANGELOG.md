# Changelog

## 0.1.0

- deterministic SAC1 catalog format
- stable artifact identifiers and metadata normalization
- exact/prefix/package/version/tag queries
- bounded local state and query output
- atomic persistence and corruption detection
- immutable snapshots and typed fail-closed errors
- zero runtime third-party dependencies
