# Changelog

## 0.1.0

- deterministic `SAB1` bundle format
- canonical path ordering
- bounded entries, metadata, and bundle size
- native SHA-256 integrity descriptors
- safe parsing and extraction
- immutable manifests and verification reports
- zero runtime third-party dependencies
