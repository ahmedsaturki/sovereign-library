# Changelog

## v0.1.0

- pure cross-platform glob compilation and matching
- `*`, `?`, segment-level `**`, and explicit escaping
- deterministic separator normalization
- explicit case and dotfile policies
- deterministic ordered include/exclude rule evaluation
- traversal-safe dot-segment normalization
- bounded matching and input limits
- immutable compiled matcher representation
- GPM1 integrity-protected serialization
- zero runtime third-party dependencies
- regression hardening for escaped metacharacters and absolute-root anchoring
