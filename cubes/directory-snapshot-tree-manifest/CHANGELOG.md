# Changelog

## v0.1.0

- deterministic recursive local directory inventory
- explicit file, directory, and symlink representation
- record-only, reject, and contained-follow symlink policies
- fail-fast, warning, and skip-vanished mutation policies
- bounded traversal, warnings, manifest size, and optional digest bytes
- optional injected content digest capability
- canonical deterministic serialization and snapshot identity
- read-only filesystem boundary
- cross-platform capability seams
- zero runtime third-party dependencies
