# Changelog

## 0.1.0

- deterministic SAG1 relationship graph format
- stable node and edge identities
- bounded adjacency and path queries
- cycle detection and optional cycle rejection
- atomic local persistence and corruption detection
- immutable snapshots and typed fail-closed errors
- zero runtime third-party dependencies
