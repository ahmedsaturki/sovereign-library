# Changelog

## 0.1.0

- canonical artifact reference parsing
- deterministic bounded local candidate resolution
- exact version, digest, tag, and unambiguous name matching
- explicit ambiguity and not-found typed errors
- immutable snapshots and fail-closed validation
- zero runtime third-party dependencies
