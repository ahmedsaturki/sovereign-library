! Configuration file for TLC.
\* Generated by scripts/formal-verify.mjs; pinned to prover version 2.20.

\* SPECIFICATION
SPECIFICATION Spec

\* INVARIANTS
INVARIANT TypeOK
INVARIANT Containment
INVARIANT RootFixed

\* CONSTANTS
CONSTANTS
    Roots = {r1, r2}
    Candidates = {c1, c2, c3}
    Symlinks = {}
    SymlinkTargets = [s1 \in Symlinks |-> "ROOT"]

\* SYMMETRY
SYMMETRY Permutations(Candidates)

\* CHECK_DEADLOCK FALSE
\* CHECK_DEADLOCK is left TRUE for FileLease.tla since liveness
\* proofs require deadlock detection.