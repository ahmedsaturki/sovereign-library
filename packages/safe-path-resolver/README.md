# @sovereign/safe-path-resolver

Deterministic path normalization, resolution, containment, canonicalization, comparison, and integrity-protected report serialization.

## Runtime

Node.js 24 LTS or newer. The package has no runtime third-party dependencies.

## Public API

The package root exposes only the symbols frozen by `docs/PUBLIC_API_BOUNDARY_V0.1.md`.
